% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_engine_conjugate.R
\name{run_trial_simulation}
\alias{run_trial_simulation}
\title{Run a full two-arm conjugate simulation}
\usage{
run_trial_simulation(
  p_control,
  p_treat,
  n_max,
  n_interim,
  a0 = 1,
  b0 = 1,
  sup_interim = 0.99,
  fut_interim = 0.1,
  sup_final = 0.975,
  n_sims = 1000,
  n_draws = 2000,
  seed = 1
)
}
\arguments{
\item{p_control, p_treat}{True control / treatment success probabilities. Set
them equal to simulate under the null (the type I error scenario).}

\item{n_max}{Maximum sample size per arm.}

\item{n_interim}{Per-arm sample size at the interim analysis (\verb{< n_max}).}

\item{a0, b0}{Beta prior hyper-parameters.}

\item{sup_interim, fut_interim, sup_final}{Decision thresholds passed to
\code{\link[=apply_decision]{apply_decision()}}.}

\item{n_sims}{Number of simulated trials.}

\item{n_draws}{Posterior Monte-Carlo draws per analysis.}

\item{seed}{Random seed for reproducibility.}
}
\value{
A list with \code{paths} (raw simulated paths), \code{per_trial} (per-trial
decisions), and \code{oc} (operating characteristics with MCSE, from
\code{\link[=summarise_oc]{summarise_oc()}}).
}
\description{
Convenience wrapper that simulates paths, applies the decision rule, and
summarises the operating characteristics in one call.
}
\examples{
res <- run_trial_simulation(0.3, 0.45, n_max = 150, n_interim = 75,
                            n_sims = 300)
res$oc

}
