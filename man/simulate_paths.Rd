% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_engine_conjugate.R
\name{simulate_paths}
\alias{simulate_paths}
\title{Simulate adaptive-trial sampling paths}
\usage{
simulate_paths(
  p_control,
  p_treat,
  n_max,
  n_interim,
  a0 = 1,
  b0 = 1,
  n_sims = 1000,
  n_draws = 2000,
  seed = 1
)
}
\arguments{
\item{p_control, p_treat}{True control / treatment success probabilities. Set
them equal to simulate under the null (the type I error scenario).}

\item{n_max}{Maximum sample size per arm.}

\item{n_interim}{Per-arm sample size at the interim analysis (\verb{< n_max}).}

\item{a0, b0}{Beta prior hyper-parameters.}

\item{n_sims}{Number of simulated trials.}

\item{n_draws}{Posterior Monte-Carlo draws per analysis.}

\item{seed}{Random seed for reproducibility.}
}
\value{
A data frame with one row per simulated trial and columns
\code{p_sup_1}, \code{est_1}, \code{lwr_1}, \code{upr_1} (interim) and \code{p_sup_2}, \code{est_2},
\code{lwr_2}, \code{upr_2} (final). Attributes \code{true_log_or}, \code{is_null}, and the
design (\code{n_max}, \code{n_interim}) are attached for downstream use.
}
\description{
Generates \code{n_sims} two-arm trials with one interim look. For every trial both
the interim posterior summary (at \code{n_interim} per arm) and the final posterior
summary (at \code{n_max} per arm) are computed, regardless of whether a real trial
would have stopped early. This makes the decision rule a pure function of the
thresholds (see \code{\link[=apply_decision]{apply_decision()}}) and lets \code{\link[=calibrate_threshold]{calibrate_threshold()}} sweep
thresholds over a single simulated data set.
}
\examples{
paths <- simulate_paths(0.3, 0.3, n_max = 150, n_interim = 75, n_sims = 200)
attr(paths, "is_null")

}
