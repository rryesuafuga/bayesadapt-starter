% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_engine_conjugate.R
\name{apply_decision}
\alias{apply_decision}
\title{Apply the adaptive decision rule to simulated paths}
\usage{
apply_decision(paths, sup_interim = 0.99, fut_interim = 0.1, sup_final = 0.975)
}
\arguments{
\item{paths}{A data frame from \code{\link[=simulate_paths]{simulate_paths()}}.}

\item{sup_interim}{Interim efficacy-stopping threshold.}

\item{fut_interim}{Interim futility-stopping threshold.}

\item{sup_final}{Final superiority threshold.}
}
\value{
A data frame with one row per trial: \code{decision} (one of \code{efficacy},
\code{futility}, \code{efficacy_final}, \code{inconclusive}), \code{declared} (logical:
superiority declared at either look), \code{stopped_interim}, \code{n_used} (total
patients across both arms), and the treatment-effect estimate \code{est_log_or}
with credible limits \code{ci_lwr}, \code{ci_upr} taken at the stopping analysis.
The \code{true_log_or} / \code{is_null} attributes are carried through.
}
\description{
Turns simulated paths (see \code{\link[=simulate_paths]{simulate_paths()}}) into per-trial decisions for a
given set of posterior-probability thresholds. The interim look can stop for
EFFICACY (\code{p_sup_1 >= sup_interim}) or FUTILITY (\code{p_sup_1 <= fut_interim});
trials that continue declare SUPERIORITY at the final look when
\code{p_sup_2 >= sup_final}.
}
\examples{
paths <- simulate_paths(0.3, 0.45, n_max = 150, n_interim = 75, n_sims = 200)
decided <- apply_decision(paths, sup_interim = 0.99,
                          fut_interim = 0.10, sup_final = 0.975)
table(decided$decision)

}
